#!/bin/zsh

# Install basic packages
echo -e "\033[32mInstall packages\033[0m"

which gpg   > /dev/null || yaourt -S --no-confirm gpg
which wget  > /dev/null || yaourt -S --no-confirm wget
which htop  > /dev/null || yaourt -S --no-confirm htop
which r2    > /dev/null || yaourt -S --no-confirm radare2
which emacs > /dev/null || yaourt -S --no-confirm emacs
